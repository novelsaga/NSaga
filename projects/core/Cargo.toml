[package]
  edition.workspace = true
  name              = "novelsaga-core"
  version.workspace = true

[lib]
  crate-type = ["rlib"]

[dependencies]
  pangu = "0.3.0"
  serde = { version = "1.0", features = ["derive"], optional = true }

[features]
  serde = ["dep:serde"]

[lints]
  workspace = true
